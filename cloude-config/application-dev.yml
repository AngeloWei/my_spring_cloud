server:
  port: 9200

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          #git地址
          uri: https://gitee.com/angelo_zou/springCloud
          #仓库路径
          search-paths: /config
          #git用户名
          username:
          #密码
          password:
      #分支
      label: master